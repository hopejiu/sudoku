# 2026-06-22

## Sudoku 奖励/成瘾系统实现

### 新增文件
- `autoload/PlayerManager.gd` — 玩家等级/经验/积分/背包管理 Autoload
- `scripts/game/ItemData.gd` — 道具数据 Resource 类（预留）
- `scripts/ui/Shop.gd` — 商店脚本
- `scenes/sudoku/Shop.tscn` — 商店场景

### 修改文件
- `project.godot` — 注册 PlayerManager Autoload
- `scripts/game/SudokuBoard.gd` — 添加 `hint_cap` 字段，`get_hint()` 检查上限
- `scripts/ui/SudokuGame.gd` — 完整重写：有限提示、道具集成、通关奖励、升级弹窗
- `scripts/ui/SudokuGrid.gd` — 添加炸弹瞄准模式（拖动选3×3宫）、闪光/爆炸特效
- `scenes/sudoku/SudokuGame.tscn` — 移除 AutoBtn、添加 ItemsBtn/ItemsPanel/LevelUpOverlay/徽章/奖励标签
- `scripts/ui/SudokuMenu.gd` — 添加商店按钮
- `scenes/sudoku/SudokuMenu.tscn` — 添加 ShopBtn
- `CONTEXT.md` — 完整更新所有新决策

### 关键设计决策
- 提示上限 = `等级 / 3 + 1`（整除）
- 经验/积分 = `难度 + 10`（通关奖励）
- 升级所需经验固定 100
- 道具：放大镜（20积分，+1提示上限/局）、炸弹（100积分，揭露3×3宫）
- 删除闪电速通（auto_btn）
- 升级时弹出全屏特效弹窗

### 角色系统设计方案（已完成头脑风暴，待实现）

**流程**: 数独菜单 → 开启新游戏 → 角色选(3选1) → 难度选(1~20) → 加载 → 游戏
**角色A** 连锁者：完成行/列/宫时自动揭露1空格 + 特效
**角色B** 明镜者：选中格时键盘自动禁用冲突数字
**角色C** 专注者：连续5正确数字 → 揭露1空格 + Combo特效
**布局**: 底部左半立绘130×184px + 右半数字键盘
**难度上限**: 1~16 → 1~20（挖空数 26~66）
**语音**: 25个触发场景预留，以 OGG + JSON 格式提供
**资源预留**: PNG立绘/头像、JSON台词、OGG语音、Live2D可选

## 自动化测试框架搭建

### GUT (Godot Unit Test) 安装
- 从 GitHub 克隆 GUT v9.6.0 到 `addons/gut/`（9.3.0 与 Godot 4.6 不兼容，Logger 阴影原生类）
- 清理不需要的示例和文档目录
- 在 `project.godot` 中启用 GUT 插件

### 测试文件结构
- `test/unit/` — 13 个 GUT 单元测试文件（extends GutTest）
- `.gutconfig.json` — GUT 配置文件
- `test/README.md` — 使用说明

### 测试覆盖范围（13 个文件）
SudokuRules、SudokuSolver、SudokuGenerator、SudokuBoard、TimerController、ItemMagnifier、ItemBomb、SkillChainReveal、SkillComboReveal、SkillConflictBlock、ProfileManager、PlayerManager、CharacterManager

### 运行方式
```powershell
# CLI 方式（GUT 9.6.0 可用）
& "D:\software\Godot\win64.exe" --headless -s addons/gut/gut_cmdln.gd

# 或 Godot 编辑器底部 GUT 面板 Run All
```

### 最终测试结果（2026-06-22）
- 13 个脚本 / 116 个测试 / 116 通过 / 931 断言 / 1.2 秒
- 6 个孤儿节点警告（CharacterManager/PlayerManager 的 Node 未释放，不影响结果）

### 修复的编译错误（2026-06-22）
- `PlayerManager.gd:189` — `var def :=` 类型推断失败，改为 `var def: Dictionary =`
- `CharacterManager.gd:68` — `_save()` 不存在，改为 `save()`
- `CharacterManager.gd:113` — `JSON.parse_string()` 返回 Variant，去掉 `:=` 类型推断
- `ItemMagnifier.gd:26` — `use()` 缺少 `args` 参数，与父类签名不匹配
- `CharacterSelect.gd:95,103` — `theme_override_font_sizes/font_size =` 语法错误，改为 `add_theme_font_size_override()`
- `Shop.gd:56,61` — 同上
- `test_sudoku_board.gd:122` — `board.notes[0][0]` 类型推断失败
- `test_sudoku_generator.gd:66` — `26 + level * 2` 类型推断失败
